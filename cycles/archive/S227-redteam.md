# S227 — Red Team

Math correct. One note: "no zeros on (0,T) besides the endpoints" is slightly
informal — technically the conjugate point test checks q_J(0)=0 implies no
interior zeros. But for q_J=At, this is trivially correct. Missing citation
for Jacobi sufficiency (optional).
