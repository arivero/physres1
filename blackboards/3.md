<!-- author: critic -->
<!-- Overwrites: OQ-FdV-3 HC proof V_HD=-|rho|^2 (mathematician, §§11-13 promoted to notebook) -->
<!-- Topic: Definitive Gilkey sign resolution — three operators, correct identification -->
<!-- Date: 2026-02-23 (R26) -->
<!-- Connects: BB1 §13 (R25 critic review), BB4 §12 (R25 computationalist fix), notebook Thm M4.1 -->

# Definitive Resolution: Half-Density Laplacian and Heat Kernel Signs

## §1. Three Operators on \(S^3\)

\(S^3\) unit radius. \(R = 6\). Spectrum of \(-\Delta_g\): \(\lambda_l = l(l+2)\), deg \((l+1)^2\).

| # | Operator | Definition | Spectrum on \(S^3\) |
|---|----------|------------|---------------------|
| O1 | \(-\Delta_g\) | bare scalar Laplacian | \(l(l+2)\) |
| O2 | conjugation | \(\|g\|^{1/4}(-\Delta_g)\|g\|^{-1/4}\) | \(l(l+2)\) (isospectrality) |
| O3 | shifted | \(-\Delta_g + V_{\mathrm{HD}} = -\Delta_g - 1\) | \(l(l+2) - 1\) |

O2 and O3 are DIFFERENT operators. O2 = O1 up to similarity. O3 = O1 shifted by \(-1\).

## §2. Isospectrality Proof

\(\Delta_{1/2} := \|g\|^{1/4}(-\Delta_g)\|g\|^{-1/4}\) is a similarity transform of \(-\Delta_g\).
Similarity preserves eigenvalues. Therefore O2 has eigenvalues \(l(l+2)\). \(\square\)

**Direct check (\(l=0\)):** Constant half-density \(\phi = \|g\|^{1/4}\cdot 1\).
\(\Delta_{1/2}\phi = \|g\|^{1/4}(-\Delta_g)(\|g\|^{-1/4}\phi) = \|g\|^{1/4}(-\Delta_g)(1) = 0\).
Eigenvalue = 0 = \(0\cdot(0+2)\). NOT \(-1\).

## §3. Why O2 \(\neq\) O3

In geodesic polar coords on \(S^3\):
\[
\Delta_{1/2}f = -\Delta_g f + V_{\mathrm{HD}}f + W^i\partial_i f
\]
where \(W^\chi = 2/\tan\chi\), \(W^\theta = 1/(\sin^2\!\chi\,\tan\theta)\).
\(V_{\mathrm{HD}} = -1 - 1/(4\sin^2\!\chi) - 1/(4\sin^2\!\chi\sin^2\!\theta)\).

\(V_{\mathrm{HD}}\) is NOT \(-1\) globally (diverges at poles). The drift \(W^i\) is nonzero away from the RNC center.

**On \(S^3 \cong \mathrm{SU}(2)\) in left-invariant coords:** \(\|g\|^{1/4} = \mathrm{const}\), so \(\Delta_{1/2} = -\Delta_g\) (no drift, no potential). Eigenvalues \(l(l+2)\).

## §4. Heat Kernel Coefficients (Spectral Sum, SymPy)

Heat trace: \(Z(t) \sim (4\pi t)^{-3/2}\mathrm{Vol}(S^3)[1 + a_1 t + a_2 t^2 + \ldots]\).

| Operator | Eigenvalues | Numerical \(a_1\) | Numerical \(a_2\) |
|----------|-------------|-------------------|-------------------|
| O1 = O2 | \(l(l+2)\) | \(1.000 = R/6\) | \(0.500 = R^2/72\) |
| O3 | \(l(l+2)-1\) | \(2.000 = R/3\) | \(2.000 = R^2/18\) |

Script: `tmp/gilkey_sign_definitive.py` (Lmax=30000, polynomial fit, \(t \in [0.0002, 0.01]\)).

## §5. The Notebook Error (Thm M4.1)

Notebook §1 line 18: "\(a_1 = R/6 + V\)" applied with \(V = V_{\mathrm{HD}} = -R/6\) gives \(a_1 = 0\).

**Two independent errors:**

**(E1) Operator identification.** "\(\Delta_{1/2} = -\Delta_g + V_{\mathrm{HD}}\)" is WRONG as an operator identity. The conjugation has drift terms. The correct decomposition: \(\Delta_{1/2} = -\Delta_g + V_{\mathrm{HD}} + W^i\partial_i\).

**(E2) Gilkey formula misapplication.** The formula \(a_1 = R/6 + V\) is for \(P = -\Delta + V\) with NO first-order terms. For an operator with drift \(b^i\partial_i\), the standard form is \(D = -(\nabla'^2 + E')\) with \(E' = V - \tfrac{1}{2}\mathrm{div}(b) + \tfrac{1}{4}|b|^2\).

## §6. Correct Endomorphism \(E'\)

At any point (using RNC):
\[
V_{\mathrm{HD}}|_0 = -R/6, \quad W^i|_0 = 0, \quad \mathrm{div}(W)|_0 = -R/3, \quad |W|^2|_0 = 0
\]
\[
E' = V_{\mathrm{HD}} - \tfrac{1}{2}\mathrm{div}(W) + \tfrac{1}{4}|W|^2 = -\tfrac{R}{6} + \tfrac{R}{6} + 0 = 0
\]

**SymPy verification on \(S^3\):** \(E' = 0\) identically at all points (not just RNC center).
Script: `tmp/pointwise_vs_integrated.py`.

**Therefore:** \(a_1(\Delta_{1/2}) = R/6 + E' = R/6 + 0 = R/6\) (any manifold, any dimension).

## §7. Correct Heat Kernel Coefficients

The half-density Laplacian (conjugation, O2) has the SAME heat kernel as the bare Laplacian (O1):
\[
a_k(\Delta_{1/2}; M) = a_k(-\Delta_g; M) \quad \forall\,k.
\]

On unit \(S^3\): \(a_1 = R/6 = 1\), \(a_2 = R^2/72 = 1/2\). NOT zero.

## §8. What About \(-\Delta_g + V_{\mathrm{HD}}\) (O3)?

O3 is a legitimate operator. On \(S^3\) (where \(V_{\mathrm{HD}} = -1\) is constant):

**Product formula:** \(Z_{\mathrm{O3}}(t) = e^t Z_{\mathrm{O1}}(t)\). Therefore:
\(a_1^{\mathrm{O3}} = a_1^{\mathrm{O1}} + 1 = R/6 + 1 = R/3 = 2\).
\(a_2^{\mathrm{O3}} = a_2^{\mathrm{O1}} + a_1^{\mathrm{O1}} + 1/2 = 1/2 + 1 + 1/2 = 2\).

**KL formula:** \(\kappa_n(3) = \delta_{n,0}\) implies \(b_{2n}^{(0)}(\bar{E}) = 0\) for \(n \geq 1\).

**Interpretation:** KL's \(b_{2n}\) may measure deviations from the shifted background \(\bar{E}\). Their relation to standard Seeley-DeWitt \(a_k\) requires clarification. The mathematician is working on this (kanban).

## §9. Status of R25 Critic Review (Self-Correction)

My R25 review (BB1 §13) was PARTIALLY CORRECT:
- (\(\checkmark\)) Thm M4.1 is wrong — \(a_1 \neq 0\) for the half-density Laplacian.
- (\(\checkmark\)) The Gilkey formula was misapplied (drift terms omitted).
- (\(\times\)) The eigenvalues \(l(l+2)-1\) are for O3, not \(\Delta_{1/2}\). My claim \(a_1 = R/3\) was for the wrong operator.
- (\(\times\)) The correct result is \(a_1 = R/6\), not \(R/3\).

## §10. Revised Collateral Assessment

| Claim | Status | Explanation |
|-------|--------|-------------|
| Thm M4.1 (\(a_1=0\) universal) | **WRONG** | \(a_1 = R/6\) (drift terms omitted) |
| KL \(\kappa_n(3)=\delta_{n,0}\) | **CORRECT** math fact | Applies to O3, not O2 |
| KL \(\Rightarrow a_k=0\) for \(\Delta_{1/2}\) | **WRONG** | O3 \(\neq\) \(\Delta_{1/2}\) |
| Corollary M1.1 (uniqueness) | **CORRECT for O3** | But O3 is not \(\Delta_{1/2}\) |
| \(V_{\mathrm{HD}} = -(d-1)R/(4d)\) | **CORRECT** | Geometry, no sign issue |
| \(V_{\mathrm{HD}}\) constant iff \(d \in \{1,3\}\) | **CORRECT** | On \(S^d\) |
| D=4 coincidences (i)-(iv) | **CORRECT** | Independent of heat-kernel claims |
| D=4 coincidence (v): \(a_2=0\) | **WRONG** | \(a_2 = R^2/72 \neq 0\) |
| D=4 coincidence (vi): all \(a_k=0\) | **WRONG** | Heat trace = bare Laplacian |

## §11. Paper Retractions Needed

**D9.1p** (manuscript line 1638): claims \(a_1(\Delta_{1/2})=0\). **WRONG.** Must retract.
**D9.1q** (manuscript line 1641): claims all \(a_k=0\) on \(S^3\). **WRONG.** Must retract.

## §12. Possible Salvage via \(\xi = 1/6\) Coupling

The operator \(P_\xi = -\Delta_g + \xi R\) with \(\xi = 1/6\) gives \(V = +R/6\), \(E = -R/6\).
\(a_1(P_{1/6}) = R/6 + (-R/6) = 0\). This IS protected. No first-order terms.

Composition forces \(\xi = 1/6\) (OQ1a, THREE-AGENT). The \(a_1=0\) story may survive under the coupling operator rather than the half-density conjugation. But this is a different mechanism: curvature coupling, not measure conjugation.

**OQ-R26-1 RESOLVED:** Composition forces the **conjugation** \(\|g\|^{1/4}(-\Delta_g)\|g\|^{-1/4}\), NOT the coupling operator \(-\Delta_g + R/6\).

Evidence: manuscript H4.0, D4.0, H4.0b explicitly define the composition-forced operator as the conjugation. Line 1602 says "the compositionally forced ordering." Line 1593 gives the identity \(\widetilde{H} = H_L + V_{\mathrm{HD}}\).

## §13. Operator Decomposition (Why D9.1p Fails)

**The correct identity** (manuscript line 1593, SymPy-verified):
\[
\|g\|^{1/4}(-\Delta_g)\|g\|^{-1/4} = H_L + V_{\mathrm{HD}}
\]
where \(H_L = -\partial_\chi^2 - \frac{1}{\sin^2\chi}(\partial_\theta^2 + \frac{1}{\sin^2\theta}\partial_\phi^2)\) acts on \(L^2(d\chi\,d\theta\,d\phi)\).

**Decomposition relative to \(-\Delta_g\):**
\[
\|g\|^{1/4}(-\Delta_g)\|g\|^{-1/4} = -\Delta_g + V_{\mathrm{HD}} + W^i\partial_i
\]
Drift: \(W^\chi = 2/\tan\chi\), \(W^\theta = 1/(\sin^2\chi\,\tan\theta)\).

**Key identity (SymPy-verified):** \(W^i = B^i\) where \(B^i\) are the connection terms of \(-\Delta_g\).
The drift from conjugation EXACTLY cancels the connection terms of \(-\Delta_g\), giving the drift-free form \(H_L + V_{\mathrm{HD}}\).

**Why D9.1p is wrong:** It writes \(a_1 = R/6 + V_{\mathrm{HD}}\) using the Gilkey formula for \(P = -\Delta_g + V\) (no drift). But the operator IS \(-\Delta_g + V_{\mathrm{HD}} + W^i\partial_i\) (WITH drift). The drift-free form \(H_L + V_{\mathrm{HD}}\) is relative to \(H_L\), not \(-\Delta_g\). Gilkey's \(R/6\) term refers to the Levi-Civita connection of \(g\), which is built into \(-\Delta_g\) but NOT into \(H_L\).

Correct treatment: absorb drift into connection, get \(E' = 0\) (§6). Result: \(a_1 = R/6\).

## §14. Collateral Damage Assessment

| Claim | Verdict | Impact |
|-------|---------|--------|
| D9.1p (\(a_1=0\) universal) | **WRONG** | Must retract. \(a_1 = R/6\). |
| D9.1q (all \(a_k=0\) on \(S^3\)) | **WRONG** | Must retract. Heat trace = bare. |
| D9.1l (V_HD formula) | **CORRECT** | Pure geometry, no HK claim. |
| D9.1n (chart dependence) | **CORRECT** | Independent of HK. |
| D9.1o (Weyl vector) | **CORRECT** | Independent of HK. |
| D9.1i (spectral comparison \(S^3\)) | **CORRECT** | Eigenvalues \(l(l+2)\) confirmed. |
| BB4 (\(a_k = 2^k/k!\) for O3) | **CORRECT for O3** | But O3 \(\neq \Delta_{1/2}\). |
| BB4 CMP9b.4 (conformal \(a_2=0\) at \(D=4\)) | **CORRECT** | Different operator, independent. |

**STATUS: OQ-R26-1 RESOLVED. THREE-AGENT (critic+physicist+mathematician) review requested on full BB3.**

## §15. Mathematician Second-Agent Review (R26)
<!-- Mathematician: 2026-02-23 R26. Second-agent review of critic's sign resolution. -->

### M15.1. Isospectrality (§§1-2): VERIFIED (algebraic proof)

The conjugation \(\Delta_{1/2} = |g|^{1/4}(-\Delta_g)|g|^{-1/4}\) defines a similarity transform. The map \(U: f \mapsto |g|^{1/4}f\) is unitary \(L^2(M, \mathrm{dvol}_g) \to L^2(M, dx)\) since
\[\int |Uf|^2\,dx = \int |g|^{1/2}|f|^2\,dx = \int |f|^2\,\mathrm{dvol}_g.\]
Therefore \(\Delta_{1/2} = U(-\Delta_g)U^{-1}\) is unitarily equivalent to \(-\Delta_g\). Spectra agree: eigenvalues \(l(l+2)\) on \(S^3\). **CONFIRMED.** \(\square\)

### M15.2. Drift Decomposition (§§3,5): VERIFIED with precision

In local coordinates: \(\Delta_{1/2}f = -\Delta_g f + V_{\mathrm{HD}}f + W^i\partial_i f\) where
\(W^i = -2|g|^{1/4}g^{ij}\partial_j(|g|^{-1/4})\). In left-invariant coords on SU(2): \(|g|=\mathrm{const}\), so \(W^i=0\) and \(V_{\mathrm{HD}}=0\). The operator IS \(-\Delta_g\). **CONFIRMED.**

In geodesic polar: \(W^i \neq 0\), \(V_{\mathrm{HD}} \neq \mathrm{const}\). The decomposition into "\(-\Delta_g + V_{\mathrm{HD}}\)" OMITS the drift — this is error (E1) in §5. **CONFIRMED.**

### M15.3. Endomorphism \(E'=0\) (§6): VERIFIED (formal computation)

The standard Laplacian form \(\Delta_{1/2} = -(g^{ij}\nabla'_i\nabla'_j + E')\) with modified connection \(\nabla' = \nabla + \omega\) where \(\omega_i = -|g|^{1/4}\partial_i(|g|^{-1/4})\). Then:
\[E' = V_{\mathrm{HD}} - \tfrac{1}{2}\mathrm{div}(W) + \tfrac{1}{4}|W|^2.\]
At RNC center (\(W=0\), \(V_{\mathrm{HD}}=-R/6\), \(\mathrm{div}(W)=-R/3\)):
\[E' = -R/6 + R/6 + 0 = 0.\]
This holds at EVERY point \(p \in M\) (choose RNC centered at \(p\)). Since \(E'\) is a scalar, coordinate-independent, \(E' = 0\) globally on ANY Riemannian manifold. No homogeneity needed. **CONFIRMED.**

**Corollary M15.1 (Universal vanishing).** \(E'(\Delta_{1/2}) = 0\) on every closed Riemannian \((M,g)\). Therefore \(a_1(\Delta_{1/2}; M) = R/6\) universally. \(\square\)

### M15.4. Consequences for BB1 §14 and BB4

My BB1 §14 analysis (KL vs SD resolution) used the physicist's eigenvalues \(l(l+2)-1\) (O3). The resolution is still MATHEMATICALLY CORRECT for O3 — but O3 is NOT \(\Delta_{1/2}\).

**Corrected summary:**
- \(\Delta_{1/2}\) (conjugation): eigenvalues \(l(l+2)\), \(a_k = 1/k!\) (= bare Laplacian). Heat trace \(= (4\pi t)^{-3/2}\mathrm{Vol}\cdot e^t\).
- O3 \(= -\Delta_g - 1\): eigenvalues \(l(l+2)-1\), \(a_k = 2^k/k!\). Heat trace \(= (4\pi t)^{-3/2}\mathrm{Vol}\cdot e^{2t}\).
- Conformal \(-\Delta_g + 1\): eigenvalues \(l(l+2)+1 = (l+1)^2\), \(a_k = 0\). Heat-kernel flat.

BB4's "correct SD coefficients \(a_k(\mathrm{HD}) = 2^k/k!\)" are for O3, not \(\Delta_{1/2}\).

### M15.5. The Critical Question (OQ-R26-1)

**Which operator does composition force?** This is the CENTRAL question.

**Argument for O2 (conjugation):** The path-integral composition law \(\int K(x,z) K(z,y)\,dz = K(x,y)\) in half-density form eliminates the measure factor. The operator governing this kernel is exactly the conjugation \(\Delta_{1/2} = |g|^{1/4}(-\Delta_g)|g|^{-1/4}\). This is the standard physics argument.

**Argument for O3 (shifted):** In RNC at any point, \(\Delta_{1/2}\) looks like \(-\Delta + V_{\mathrm{HD}} + O(\text{drift})\). The drift vanishes at the RNC center, so locally \(\Delta_{1/2}|_p \approx -\Delta_g + V_{\mathrm{HD}}(p)\). The Seeley-DeWitt expansion at \(p\) uses the LOCAL operator, and the drift contributes through \(E'\).

**Resolution (mathematical):** The correct operator is O2 (conjugation). But its heat kernel coefficients are computed by the STANDARD Gilkey formula applied to the operator in Laplacian form — i.e., after absorbing the drift into a modified connection. The endomorphism is \(E' = 0\), giving \(a_1 = R/6\). The naive formula "\(a_1 = R/6 + V_{\mathrm{HD}}\)" is WRONG because it ignores the drift.

**The composition axiom produces O2.** The spectral consequences are:
- Eigenvalues = bare Laplacian eigenvalues (composition is spectrally invisible)
- \(a_1 = R/6\) (not \(0\), not \(R/3\))
- Heat-kernel coefficients = bare Laplacian coefficients

### M15.6. Revised Impact Assessment

| Previous claim | Status |
|----------------|--------|
| \(a_k(\Delta_{1/2}) = 2^k/k!\) (BB4) | **WRONG** — these are for O3 |
| \(a_k(\Delta_{1/2}) = 1/k!\) | **CORRECT** (isospectrality) |
| \(a_1 = R/3\) (Critic R25, BB1 §13) | **WRONG** — for O3, not \(\Delta_{1/2}\) |
| \(a_1 = R/6\) (Critic R26, BB3 §7) | **CORRECT** |
| KL \(\kappa_n(3)=\delta_{n,0}\) for O3 | **CORRECT** math (applies to O3) |
| KL for \(\Delta_{1/2}\) | **INAPPLICABLE** (\(\Delta_{1/2}\) has drift, not constant-shift) |
| Conformal scalar heat-kernel flat | **CORRECT** (\(a_k = 0\)) |
| BB1 §14 (KL vs SD resolution) | **CORRECT for O3**, must re-scope to O3 |
| D9.1p/q manuscript retractions | **NEEDED** (as critic §11 states) |

### M15.7. Remark: What "V_HD" Actually Is

\(V_{\mathrm{HD}} = -|g|^{1/4}\Delta_g(|g|^{-1/4})\) is a FUNCTION on \(M\), well-defined coordinate-independently. But the decomposition \(\Delta_{1/2} = -\Delta_g + V_{\mathrm{HD}}\) is FALSE — it omits the drift. The correct decomposition:
\[\Delta_{1/2} = -\Delta_g + V_{\mathrm{HD}} + W^i\partial_i\]
and the Seeley-DeWitt endomorphism is \(E' = 0\) (not \(-V_{\mathrm{HD}}\)). The function \(V_{\mathrm{HD}}\) is physically meaningful (it appears in the radial potential in geodesic polars) but does NOT equal the Gilkey endomorphism.

### M15.8. Verdict

**APPROVE BB3 §§1-12.** All claims verified. The O2 vs O3 distinction is CORRECT and CONSEQUENTIAL. OQ-R26-1 is the right question; the answer is O2 (conjugation). This requires revision of BB4, BB1 §14, and the manuscript.

**SEV-1 (CRITICAL):** BB0 Prop M0.1 ("V_HD constant on bi-invariant Lie groups") is CORRECT as a statement about the FUNCTION V_HD, but MISLEADING: V_HD does not determine the heat kernel coefficients (drift cancels it out in E'). The Seeley-DeWitt endomorphism of \(\Delta_{1/2}\) is \(E'=0\) universally, regardless of V_HD.

**STATUS: TWO-AGENT (critic + mathematician). APPROVE. OQ-R26-1 answered: composition forces O2 (conjugation).**

---

## §16. Implications for D=4 Coincidence Table (Mathematician, R26)

### §16.1. Revised Table

With \(\Delta_{1/2} =\) O2 (conjugation, isospectral to \(-\Delta_g\)), Corollary M15.1 (\(E'=0\)):

| # | Coincidence | Status | Reason |
|---|---|---|---|
| (i) | \((\partial\sigma)^2\) cancellation at \(D=4\) | **SURVIVES** | About \(V_{\mathrm{HD}}\) function, not \(a_k\) |
| (ii) | \(\xi_{\mathrm{eff}} = 1/6\) at \(D=4\) | **SURVIVES** | About \(V_{\mathrm{HD}}\) at RNC center |
| (iv) | \(V_{\mathrm{HD}}\) on \(S^{D-1}\) constant iff \(D=4\) | **SURVIVES** | About \(V_{\mathrm{HD}}\) function |
| (v) | \(a_2(\Delta_{1/2}; S^3) = 0\) | **DEAD** | \(a_2(\Delta_{1/2}) = a_2(-\Delta_g) = R^2/72 \neq 0\) |
| (vi) | all \(a_k(\Delta_{1/2}; S^3) = 0\) | **DEAD** | \(a_k = a_k(-\Delta_g) = 1/k!\) |
| (v') | \(a_2(-\Delta+R/6; S^d) = 0\) iff \(d=3\) | **SURVIVES** | CMP9b.4 for conformal operator (independent) |
| (vi') | all \(a_k(-\Delta-1; S^3) = 0\) (O3) | **SURVIVES for O3** | KL+Bernoulli; O3 is a valid operator, just not \(\Delta_{1/2}\) |

### §16.2. What V_HD Still Means

\(V_{\mathrm{HD}}\) is NOT the Gilkey endomorphism, but it IS:
1. The quantum-mechanical potential in geodesic polar Schrödinger equation
2. The spectral shift in the \(H_L\) frame: eigenvalues of \(\Delta_{1/2}\) relative to \(H_L\) are \(l(l+2) = \lambda_l^{H_L} + V_{\mathrm{HD}}\) (where \(\lambda_l^{H_L}\) are eigenvalues of \(-H_L\))
3. The Harish-Chandra radial potential (BB3 old §§4-8, promoted to notebook)

The constancy of \(V_{\mathrm{HD}}\) on \(S^3\) means: in the coordinate Laplacian frame, the ordering correction is a pure shift. This is coincidence (iv) — it survives because it's about \(V_{\mathrm{HD}}\), not about \(a_k\).

### §16.3. What Changes in the Paper Narrative

**Before:** "Composition forces \(\Delta_{1/2}\), which has \(a_1 = 0\) and all \(a_k = 0\) on \(S^3\)." (WRONG)

**After:** "Composition forces \(\Delta_{1/2} = |g|^{1/4}(-\Delta_g)|g|^{-1/4}\), which is unitarily equivalent to \(-\Delta_g\). The half-density measure does not alter the spectrum or heat kernel coefficients. The physical content is in the POTENTIAL \(V_{\mathrm{HD}}\), which governs the ordering correction in the coordinate-frame Hamiltonian, and is constant on \(S^3\) uniquely at \(D=4\)."

**Surviving narrative:** The half-density prescription is the unique composition-forced measure. It does NOT "protect" against curvature corrections in the heat kernel. It DOES provide a canonical ordering (\(V_{\mathrm{HD}}\)) that is maximally simple on \(S^3\).

**STATUS: Mathematician R26. Needs physicist review for physical interpretation.**
